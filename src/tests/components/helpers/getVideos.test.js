import { getVideos } from '../../../helpers/getVideos'
import { data } from '../../../mock/mockedUTube'

// const fetchMock = jest
//     .spyOn(global, 'fetch')
//     .mockImplementation(() => Promise.resolve({ json: () => Promise.resolve([]) }))

describe('Test for getVideos', () => {
    
    test('should receive OK response', async() => {
      
      // const json = getVideos('Pipo');

      // expect(fetchMock).toBeCalled();

    });
});   

